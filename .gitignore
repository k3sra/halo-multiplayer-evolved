# Build output. Everything here is reproducible from source with build.bat.
build/
*.obj
*.pdb
*.ilk
*.exp
*.lib
*.dll

# Local logs and scratch files.
*.log
build_clean.txt

# Editor and OS noise.
.vs/
.vscode/
*.user
Thumbs.db
desktop.ini

# The log collector address. Kept out of the repo on purpose: this repo is public,
# and the address is both readable by anyone who finds it and floodable by anyone who
# wants to push the testers own reports out of a free tier.
testing/
